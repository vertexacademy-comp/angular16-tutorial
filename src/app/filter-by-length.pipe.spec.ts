import { FilterByLengthPipe } from './filter-by-length.pipe';

describe('FilterByLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
